.DS_Store

# Scratch / build-time junk that must never be committed: source art packs
# (e.g. the Kenney FBX rig that tools/sprite-render.html bakes into
# assets/textures/characters/), one-off scripts, local experiments. The game
# ships only its baked outputs, so nothing here is needed at runtime.
_tmp/
tools/.isml-cache/
# `bml --examples` writes here. It is the tool's OUTPUT, and it was committed
# into the BML repo by accident in a test run, which then made `bml --examples`
# refuse in a fresh clone because the directory it wanted to create was there.
bml-examples/

# The version record and the working plan. Both stay on disk and stay useful;
# neither ships. A changelog is a reconstruction of the order things were built
# in, one line per version, and the plan dates its entries and names people.
CHANGELOG.md
docs/PLAN.md
# …and every other planning document. Same reason: a plan dates its entries,
# quotes people by name and describes how the thing is actually built, none of
# which belongs in a repo that is itself part of the fiction. This is a pattern
# rather than a list because naming them one at a time is how two of them ended
# up committed (character-3d-plan.md in v1.8, terrain-3d-plan.md since August).
# They stay on disk and stay useful; they do not ship.
docs/*-plan.md
docs/*-PLAN.md
